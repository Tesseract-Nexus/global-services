# ==================================
# Tenant Service - Environment Variables
# ==================================
# Copy this file to .env for local development
# For production, use secret management (AWS Secrets Manager, Vault, etc.)

# ==================================
# Server Configuration
# ==================================
PORT=8086
GIN_MODE=debug  # Options: debug, release (use 'release' in production)

# ==================================
# Database Configuration
# ==================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=dev
DB_PASSWORD=devpass
DB_NAME=tesseract_hub
DB_SSLMODE=disable  # Options: disable, require (use 'require' in production)

# ==================================
# Verification Service Integration
# ==================================
# URL of the verification-service
VERIFICATION_SERVICE_URL=http://localhost:8088

# API key to authenticate with verification-service
# This MUST match the API_KEY in verification-service
VERIFICATION_SERVICE_API_KEY=tesseract_verification_dev_key_2025

# ==================================
# NOTES
# ==================================
# For Production:
# - Set GIN_MODE=release
# - Set DB_SSLMODE=require
# - Use a managed database (AWS RDS, Cloud SQL, etc.)
# - Update VERIFICATION_SERVICE_URL to production URL
# - Use secrets management instead of .env file
# - Ensure VERIFICATION_SERVICE_API_KEY matches verification-service
#
# Dependencies:
# - verification-service must be running on VERIFICATION_SERVICE_URL
# - PostgreSQL database must be running and accessible
# - location-service (for countries/states data)
#
# API Endpoints:
# - Health: GET /health
# - Ready: GET /ready
# - Onboarding: POST /api/v1/onboarding/sessions
# - Templates: GET /api/v1/onboarding/templates
# - Verification: POST /api/v1/onboarding/sessions/:id/verification/email
