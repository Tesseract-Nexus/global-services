# ==================================
# Location Service - Environment Variables
# ==================================
# Copy this file to .env for local development
# For production, use secret management (AWS Secrets Manager, Vault, etc.)

# ==================================
# Server Configuration
# ==================================
PORT=8087
GIN_MODE=debug  # Options: debug, release (use 'release' in production)

# ==================================
# Database Configuration (Optional)
# ==================================
# Database is OPTIONAL - service can run without it using mock data
# When database is configured, countries/states data will be seeded automatically
DB_HOST=localhost
DB_PORT=5432
DB_USER=dev
DB_PASSWORD=devpass
DB_NAME=tesseract_hub
DB_SSLMODE=disable  # Options: disable, require (use 'require' in production)

# ==================================
# Geo-Location Provider (Optional)
# ==================================
# Provider to use for IP-based geo-location detection
# Options: mock, maxmind, ipapi
GEO_PROVIDER=mock

# MaxMind Geo IP Configuration (if using maxmind provider)
# Get license key from: https://www.maxmind.com/
MAXMIND_LICENSE_KEY=

# IP-API Configuration (if using ipapi provider)
# Get API key from: https://ipapi.com/
IPAPI_KEY=

# ==================================
# NOTES
# ==================================
# Service Features:
# - Countries and states data (pre-seeded in database)
# - Currency information
# - Timezone information
# - IP-based geo-location detection (optional)
#
# Database:
# - Optional: Service works in mock mode without database
# - When configured: Auto-seeds countries/states on first run
# - Contains 250+ countries and 5000+ states/provinces
#
# For Production:
# - Set GIN_MODE=release
# - Set DB_SSLMODE=require
# - Use a managed database (AWS RDS, Cloud SQL, etc.)
# - For geo-location: Use maxmind or ipapi provider
# - Use secrets management instead of .env file
#
# API Endpoints:
# - Health: GET /health
# - Ready: GET /ready
# - Countries: GET /api/v1/countries
# - States: GET /api/v1/countries/:code/states
# - Currencies: GET /api/v1/currencies
# - Timezones: GET /api/v1/timezones
# - Detect Location: GET /api/v1/detect (from IP)
